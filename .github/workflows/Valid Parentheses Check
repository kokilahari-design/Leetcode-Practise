class Solution(object):
    def isValid(self, s):
        d = { '(':')' , "{":'}' , "[":"]" }
        stack = []
        for i in s:
            if i in d.keys():
                stack.append(i) 
            else:
                if stack == []:
                    return False
                else:
                    pop_value = i
                    if d[stack[-1]] == pop_value :
                        stack.pop()
                    else:
                        return False
        if stack == []:
            return True
        else:
            return False
result = Solution("()[]{}")
print(result)
